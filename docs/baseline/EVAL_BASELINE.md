# Import-eval baseline (OFFLINE) - the BEFORE picture

Every future corpus/armor pack diffs against this table.

- Date: 2026-07-16
- Commit: `d28c8a1` (post-cleanse tip; eval script, samples, and goldens all at this sha)
- Command: `node node_modules/tsx/dist/cli.mjs scripts/import-eval.mts` (equivalently `pnpm import:eval`)
- Mode: OFFLINE (default, no `--live` flag) - re-parses every sample and diffs against the
  golden set with the same parser on both sides. NO model calls, NO network (`baseUrl: null`
  in the results dump confirms).
- Results dump: `docs/audit/import_eval_results.json` (regenerated by the run; the working-tree
  copy was restored afterward - this baseline doc is the durable record).

## Summary metrics by LOB

| LOB | golden fields | F1 | numeric exact | extras rate | fabrication rate | parent-edge recall | form-attach recall | ldTableRefResolutionRate |
|---|---|---|---|---|---|---|---|---|
| GL | 5,515 | 1.0000 | 1.0000 | 0 | 0 | 1.000 | 1.000 | 0.800 (12/15) |
| IM | 19,461 | 1.0000 | 1.0000 | 0 | 0 | 1.000 | 1.000 | n/a (0 ldRefs in golden) |
| PR | 26,053 | 1.0000 | 1.0000 | 0 | 0 | 1.000 | 1.000 | n/a (0 ldRefs in golden) |
| CORE | 5,065 | 1.0000 | 1.0000 | 0 | 0 | 1.000 | 1.000 | n/a (0 ldRefs in golden) |

All four LOBs PASS the offline gate (targets: F1 >= 0.95, numeric >= 0.98, extras = 0
offline, link edges >= 0.98).

## Notes

- **Citations**: the citation / citation-resolution metrics are LIVE-mode-only (they replay
  the brain's provenance rows from a live run dump). Offline mode has no citation number to
  record; the standing live targets are citation = 1.00, citationResolve >= 0.98. A live
  baseline requires `--live` + BASE_URL/IMPORT_USER/IMPORT_PASS/IMPORT_TENANT and spends
  model budget - deliberately NOT run for this baseline per the zero-spend constraint.
- **Known ldTableRef gap (GL 0.800)**: three GL rules cite limit/deductible tables that
  resolve to no extracted table (e.g. `GL.RU.024` -> `LDTable.122`). This is a pre-existing
  golden-set fact at `d28c8a1`, not introduced by this pack; it is the one non-1.0 number a
  future pack could move.
- The corpus-2026-07 hagerty fixtures are NOT part of this eval's sample set; they enter the
  picture when the build pack wires them into goldens. This table is purely the pre-existing
  GL/IM/PR/CORE sample -> golden stability picture.
