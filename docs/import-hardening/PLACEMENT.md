# Import-hardening corpus — placement map

Corpus root: `samples/hardening/`. Staged by the operator from the Claude.ai
project-knowledge export (`README_CORPUS.md` + `CHECKSUMS.md5` at repo root are the
export's own inventory; every path below is checksum-verified against `CHECKSUMS.md5`).

| Directory | Contents | Purpose |
|---|---|---|
| `workbooks/` | 16 XLSX product workbooks: GL set (`20ISO*GL` ×4 + byte-identical `sampleGL*` ×4), IM (`sampleIM*` ×2, `Product_Framework__SECURA__Inland_Marine`, `Inland_Marine_Rules_Repository__SECURA__Master`), PR (`samplePR*` ×2, `Property_Rating_Repository__Master`), PCM coverages (`Product_Component_Model_Framework_Coverages_1`) | Happy-path + real-carrier structural variety for stages 0–7 |
| `adversarial/` | `1._Product_Framework_-_SECURA_-_Property` (extensionless ZIP/OOXML that **fails full ExcelJS parse** — magic-byte routing + reader-error fixture), `Product_Framework__SECURA__Property_RF.xlsm` (macro container), `Insurance_Product___Process_Value_Explorer_Export.xlsx` (wrong-domain export), `Product_Component_Model_Framework_Coverages.xlsx` (byte-identical twin of the copy in `workbooks/` — exact-dup detection) | Router/reader robustness, dup detection, wrong-domain refusal |
| `pdf/` | HO3 trio (`20BaseFormHO3Homeowners`, `samplePHbaseformHO3`, `Homeowners__HO3` — identical byte size 4,982,942, three distinct md5s: defeats size-based dedupe), NJ filing pair (`NJ_HO_Manual_02_27_24`, `NJ_HO_Rate_Order_of_Calculations`), `LEM_03_05_23_Lemonade_Homeowners_FINAL`, `PP_00_01_06_98` (Personal **Auto** base form — anti-HO3-hardcoding fixture, see ledger F18), `PersonalAuto_WA_Rate_Washington_Auto_Rate_Manual_Version_2_11` | Filing-path fixtures incl. non-PH lines |
| `mixed/` | `mixed_requests.json` — request-level combinations **referencing** files above (no binary duplication): workbook+PDF in one upload (mixed-upload warning path), multi-workbook merge, exact-dup pair in one request | Multi-document request semantics |
| `manifest/` | `xlsx_ooxml_summary.json` — generated structural ground truth (see below) | Independent OOXML facts the pipeline must not silently lose |

## Manifest (`manifest/xlsx_ooxml_summary.json`)

Generated by `node scripts/hardening-manifest.mjs` (deterministic; re-run after adding
files). Covers `workbooks/` + `adversarial/` **plus the repo-native Tier-A goldens in
`samples/iso/`** (the CORE reference workbook lives there). Per file: md5, container
verdict; per sheet: hidden state, reported vs **true** used range, non-empty cell count,
merged ranges, independent header-row candidates (deliberately not the pipeline's own
scorer — non-circular), and an `exceedsEmbedCap` flag against the pipeline's
`MAX_EMBED_ROWS=2000` / `MAX_EMBED_COLS=128`.

Headline facts recorded (2026-07-14): **28/29 workbooks parse, 218 sheets, 29 hidden,
1 sheet exceeds the embed cap** — CORE `Rule References`, **visible, 2280×29** (280 rows
past the AI-path cap; ledger F09/F10), CORE carries **16 hidden sheets** incl.
`Core Rating Specifications (2)` (hidden, 639×73). The extensionless SECURA property
file is a ZIP container whose full parse throws (`End of data reached`) — recorded as
`error` in the manifest, kept deliberately.

## Known duplicate facts (from the export README, checksum-verified)

- `Product_Component_Model_Framework_Coverages.xlsx` (adversarial) ≡ `..._1.xlsx`
  (workbooks) — **exact** dup (md5 `ca729a8b…`); a true *near*-dup pair does not exist
  in the staged set (IH2: mutate one copy if near-dup detection needs a fixture).
- `sampleGL{framework,rules,pricing,forms}` ≡ `20ISO{Framework,Rules,Pricing,Forms}GL`
  byte-for-byte (Tier B double-counts GL — do not treat as 8 independent workbooks).
- HO3 PDF trio: same size, different content — keep all three.

## Not committed

`hardening-corpus.zip` + `hardening-corpus/` at repo root are the operator's unzip
leftovers (partial duplicate of this corpus) — left untracked, not part of the corpus.
`docs/design-review/` is another lane's untracked file — not touched.

## Offline enumeration pass

`node scripts/import-enumerate.mjs` — runs every file in `workbooks/` + `adversarial/`
(plus `mixed/mixed_requests.json` combinations) through the REAL `unifiedImport`
handler (stages 0–7 + normalizeBundle) with global `fetch` stubbed to a deterministic
minimal-valid AI (offline, $0). Records per file: routing, stage counts, plan entity
counts, warnings/errors, and a conservation summary. Results →
`docs/import-hardening/RESULTS/corpus-baseline.json`. Baseline only — **not** wired
into the golden pass/fail gate (IH2 gates it).
