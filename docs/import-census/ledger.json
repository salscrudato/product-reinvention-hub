{
  "pack": "census-era",
  "created": "2026-07-16T00:00:00Z",
  "rows": [
    {
      "id": "CE1-S1",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Preconditions verified. Lineage reconciled: prompt said d28c8a1/feat-import-concept-linker; actual tip is main@0ad8689 (d28c8a1 IS an ancestor; concept-linker branch already merged). Worktree created from main. All 6 reference workbooks present (untracked client data in main tree, copied into worktree, MUST NEVER BE COMMITTED) + 4 hagerty fixtures (tracked). Model confirmed claude-fable-5, no fallback. Baseline gate GREEN on Node 24 after installing server/ npm deps (server is outside the pnpm workspace; fresh worktrees need npm install --prefix server): typecheck+lint pass, test 152 files / 1664 tests green in 115s, build green. fast-check absent everywhere (S9 adds root dev-dep). CRASH_CENSUS.md lives at docs/baseline/ not docs/reveng/ (prompt path stale). data.js validator is at :354-363 not :243 (drifted). CORPUS NOTE: 6 reference workbooks + 4 hagerty, NOT 17 (memory says 17 ref workbooks PENDING).",
      "commit": "b3c5631",
      "ts": "2026-07-16T08:40:00Z"
    },
    {
      "id": "CE1-S2",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "docId unification (BACKLOG item 1 / R1). refIdToDocId in shared/src/insurance/refId.ts (case-preserving dot->dash), dashId re-exported as alias; isoImport.ts consumes it (private copy deleted); exported through brain-server-entry.ts -> import-brain-shared.cjs. stage7-plan.js toDocId lowercasing DELETED for refIds (label fallbacks keep legacy slug - never parentId targets); unified-import.js:374 mint now refIdToDocId. data.js validator extended with third lowercase candidate (historical docs). LOCKS: (a) refId.test.ts walks 142 seed refIds (PH/PA/GL incl RT/LD record keys) - byte-equal alias + replace-semantics + injectivity + cross-ref resolution; (b) docid-csv-childpersist.test.ts drives 2-row CSV brain output (isoPlan null) through buildImportPlan + REAL data.js envelope with in-memory cosmos - child persists, INVALID_PARENT for genuinely-missing parent preserved, legacy-lowercase parent rescued by belt-and-braces; (c) 4 canaries exact (30 tests), offline eval 4/4 F1=1.0, goldens regenerated BYTE-IDENTICAL (git diff 0 lines). DEVIATIONS: brain-server-entry.ts touched (1-line export; required for bridging, not strictly in allowlist); 'fil-prod' literal at unified-import.js:397 left as-is (twin literal in stage-filing.js:517 is outside allowlist - CE3 handoff); dashId private copy in filing/reconcile.ts:55 untouched (filing canary territory, outside allowlist - CE3 handoff).",
      "commit": null,
      "ts": "2026-07-16T09:05:00Z"
    },
    {
      "id": "CE1-S3",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Parser armor (R5 / CRASH F-3). inspectOoxmlContainer in workbook.js: EOCD+CEN walker (zip64-aware, declared sizes only, zero inflation) enforcing env-tunable ceilings totalUncompressedBytes 300MB / entryCount 2000 / perEntryCompressionRatio 400 (4MB floor - tiny XML parts legitimately deflate >100x) / declaredCellCount 3M / parseWallClockMs 180s; breach = structured IMPORT_413 {code,reason,limits} before ExcelJS load. DESIGN DECISION: declaredCellCount estimated from declared uncompressed sheet-XML bytes / 24 (NOT the dimension element - dimension-based counting would reject the legit phantom-row files the used-range clamp saves: All Lines Rules Repository reports 1,048,417 rows). Wall-clock = Promise.race (bounds waiting, cannot abort CPU-bound ExcelJS mid-flight - honest limitation, child-process isolation is the harness complement). Ceilings validated empirically against all 10 corpus files: worst legit ratio 18.1x, worst cellEst 286,571, worst total 9.6MB - zero false positives. F-0 residual closed: collectWorkbookSignals exported from seam-free workbook.js, crash-census.mjs replica deleted (--one child mode re-verified), stage0-router twin pinned by byte-identical source tripwire test. L4 landed: synthetic >1M-reported/1,609-real phantom clamps + real All Lines corpus clamp (skipIf-guarded). 13 tests green.",
      "commit": null,
      "ts": "2026-07-16T09:05:00Z"
    }
  ]
}
