{
  "schema": "ce-ledger/v1",
  "pack": "census-era",
  "created": "2026-07-16T00:00:00Z",
  "note": "rows: {id,lane,status,note,commit,ts}; union of ce1+ce2 lane ledgers merged by CE3 (first wave-2 writer) on 2026-07-16; encoding normalized UTF-16LE -> UTF-8",
  "rows": [
    {
      "id": "CE1-S1",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Preconditions verified. Lineage reconciled: prompt said d28c8a1/feat-import-concept-linker; actual tip is main@0ad8689 (d28c8a1 IS an ancestor; concept-linker branch already merged). Worktree created from main. All 6 reference workbooks present (untracked client data in main tree, copied into worktree, MUST NEVER BE COMMITTED) + 4 hagerty fixtures (tracked). Model confirmed claude-fable-5, no fallback. Baseline gate GREEN on Node 24 after installing server/ npm deps (server is outside the pnpm workspace; fresh worktrees need npm install --prefix server): typecheck+lint pass, test 152 files / 1664 tests green in 115s, build green. fast-check absent everywhere (S9 adds root dev-dep). CRASH_CENSUS.md lives at docs/baseline/ not docs/reveng/ (prompt path stale). data.js validator is at :354-363 not :243 (drifted). CORPUS NOTE: 6 reference workbooks + 4 hagerty, NOT 17 (memory says 17 ref workbooks PENDING).",
      "commit": "b3c5631",
      "ts": "2026-07-16T08:40:00Z"
    },
    {
      "id": "CE1-S2",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "docId unification (BACKLOG item 1 / R1). refIdToDocId in shared/src/insurance/refId.ts (case-preserving dot->dash), dashId re-exported as alias; isoImport.ts consumes it (private copy deleted); exported through brain-server-entry.ts -> import-brain-shared.cjs. stage7-plan.js toDocId lowercasing DELETED for refIds (label fallbacks keep legacy slug - never parentId targets); unified-import.js:374 mint now refIdToDocId. data.js validator extended with third lowercase candidate (historical docs). LOCKS: (a) refId.test.ts walks 142 seed refIds (PH/PA/GL incl RT/LD record keys) - byte-equal alias + replace-semantics + injectivity + cross-ref resolution; (b) docid-csv-childpersist.test.ts drives 2-row CSV brain output (isoPlan null) through buildImportPlan + REAL data.js envelope with in-memory cosmos - child persists, INVALID_PARENT for genuinely-missing parent preserved, legacy-lowercase parent rescued by belt-and-braces; (c) 4 canaries exact (30 tests), offline eval 4/4 F1=1.0, goldens regenerated BYTE-IDENTICAL (git diff 0 lines). DEVIATIONS: brain-server-entry.ts touched (1-line export; required for bridging, not strictly in allowlist); 'fil-prod' literal at unified-import.js:397 left as-is (twin literal in stage-filing.js:517 is outside allowlist - CE3 handoff); dashId private copy in filing/reconcile.ts:55 untouched (filing canary territory, outside allowlist - CE3 handoff).",
      "commit": "9212101",
      "ts": "2026-07-16T09:05:00Z"
    },
    {
      "id": "CE1-S3",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Parser armor (R5 / CRASH F-3). inspectOoxmlContainer in workbook.js: EOCD+CEN walker (zip64-aware, declared sizes only, zero inflation) enforcing env-tunable ceilings totalUncompressedBytes 300MB / entryCount 2000 / perEntryCompressionRatio 400 (4MB floor - tiny XML parts legitimately deflate >100x) / declaredCellCount 3M / parseWallClockMs 180s; breach = structured IMPORT_413 {code,reason,limits} before ExcelJS load. DESIGN DECISION: declaredCellCount estimated from declared uncompressed sheet-XML bytes / 24 (NOT the dimension element - dimension-based counting would reject the legit phantom-row files the used-range clamp saves: All Lines Rules Repository reports 1,048,417 rows). Wall-clock = Promise.race (bounds waiting, cannot abort CPU-bound ExcelJS mid-flight - honest limitation, child-process isolation is the harness complement). Ceilings validated empirically against all 10 corpus files: worst legit ratio 18.1x, worst cellEst 286,571, worst total 9.6MB - zero false positives. F-0 residual closed: collectWorkbookSignals exported from seam-free workbook.js, crash-census.mjs replica deleted (--one child mode re-verified), stage0-router twin pinned by byte-identical source tripwire test. L4 landed: synthetic >1M-reported/1,609-real phantom clamps + real All Lines corpus clamp (skipIf-guarded). 13 tests green.",
      "commit": "415efd2",
      "ts": "2026-07-16T09:05:00Z"
    },
    {
      "id": "CE1-S4",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Cell census core (shared/src/import/census/): CellRecord (ref/type/fnv1a64 valueHash/verbatim raw-untrimmed capped 512+flag/merge anchor-span/format bold-filled-indent-topBorder/hidden), SheetCensus (dims/nonEmpty/headerSig+sampleHash fingerprint/TableRegions), gap segmentation (blank-run>=2 OR disjoint column band; single blank row does NOT split; header per region via the EXISTING stage-2 scorer), merge normalization (anchor carries value, shadows auto-accounted MERGE_SHADOW), accounting ledger (substanceCoverage=(FACT+SCHEMA)/(nonEmpty-NOISE-HEADER-MERGE_SHADOW), posts to unknown refs rejected, shadows immutable, conservation THROWS if broken). Server feeder collectRawCensusSheets + readWorkbookCensus in workbook.js (armor-first, one ExcelJS pass, live formatting + dataValidations). 23 unit tests incl. the merge double-count pin (hostile-review Q3). Smoke on real SECURA: 19,260 cells, conservation exact, Forms View - MTG hidden = 2,072 cells (prediction confirmed to the cell).",
      "commit": "c938f15",
      "ts": "2026-07-16T09:25:00Z"
    },
    {
      "id": "CE1-S5",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Detectors a-i (census/detectors.ts, all pure + table-driven tests, 16 tests): (a) headerLockV2Signals (bold-over-plain formatting shift, string-over-numeric type shift, caller-supplied alias hits) + augmentHeaderCandidates (bounded bonuses over the EXISTING scorer output, clamped, re-sorted - feeds stage-2, never replaces); (b) repeatingParentRuns (>=35% consecutive-repeat columns); (c) staircaseHierarchy (real indent format first, leading-space ladder fallback); (d) formTokenCensus (FORM_TOKEN_GENERAL corpus grammar: 2-3 letters + 2-4 groups w/ alphabetic middle group variants + 3-4 digit block, unioned with conceptMatch carrier FORM_TOKEN, distinct counts = counting-invariant lower bounds); (e) stateLexicon (STATE_COLUMNS matrix vs STATE_LIST orientation, reuses layoutDetector lexicon); (f) idColumnProfile (dot-grammar shapes incl. IM.COV044.00 / PR.COV001.0, per-workbook prefix census); (g) nearDuplicateSheetClusters (headerSig basis + version-word name basis; REPORT ONLY, fold policy is CE3); (h) harvestAliasOverlay (Definitions sheets via existing parser + Data Validation list domains -> CensusAliasOverlay + schemaCellRefs for SCHEMA posting; backlog-15 groundwork); (i) hiddenSheetSubstance. Bridged through brain-server-entry (import-brain-shared.cjs rebuilt).",
      "commit": "c938f15",
      "ts": "2026-07-16T09:25:00Z"
    },
    {
      "id": "CE1-S7",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "cell-census.mjs CLI (crash-census discipline: child-process isolation, CELL_CENSUS_TIMEOUT_MS default 120s, zero model calls/network, findings-preserving markdown) run over all 10 corpus files: ZERO crashes, conservation EXACT everywhere. Baseline artifacts docs/import-census/BASELINE/*.census.json + CELL_CENSUS.md. Recorded findings F-C1..F-C8 incl.: PCM version trio clusters confirmed (counting-basis delta ~14 cells vs pack numbers), SECURA MTG 2,072 exact, SECURA Ref Connect Pull 3 stacked regions over 4,983x41, All Lines census costs 1.3GB child RSS (CLI-only; CE3 needs formatting-lite mode before live wiring). EXPECTATION FAILED HONESTLY: the predicted 148-col Client Master PCM does not exist in this corpus (observed 689x71 reported / 70 real; nothing exceeds 128 real cols) - the >128 embed-cap loss case is untestable here, CE3 needs a synthetic or the missing 11 reference workbooks. SEQUENCING NOTE: S7 committed before S6 (self-contained; S6 follows).",
      "commit": "740a463",
      "ts": "2026-07-16T09:25:00Z"
    },
    {
      "id": "CE1-S6",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "consumedSpans instrumentation. ConsumedSpanRec + Ctx.consume() in isoImport (no-op unless a collector is passed as mapIsoWorkbook's new OPTIONAL third param); block-grain spans inside parseLdTables/parseRtTables (marker row -> block end, marker->value/comment cols) and detectReferenceTables (draft start..end); sheet-grain spans for framework/forms/dynamic/rules/formRules/rating (only when the parse produced output). PROOF output-identical (three independent): (1) tests/import/consumed-spans.test.ts serialize-diff bare-vs-instrumented on GL/IM/PR/CORE via the REAL server reader grids - 4/4 green in 21s; (2) --write-golden then git diff tests/golden/import = 0 lines, sha256s match committed bytes; (3) offline eval 4/4 F1=1.0 post-edit. PERF FINDING: the first test draft used the eval-style rowCount flatten and CORE walked 286s of PHANTOM cells per read (parse itself is 120ms) - rewrote against readWorkbookToStructural (true-extent), whole suite 21s; the import-eval.mts reader slowness is recorded as a CE2 note, not fixed here (eval2 namespace).",
      "commit": "8a9f19c",
      "ts": "2026-07-16T09:55:00Z"
    },
    {
      "id": "CE1-S8",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "L1-L7 locks landed (L4 rode S3 in parser-armor.test.ts). tests/import/lock-candidates.test.ts (18 tests): L1 splitList table-driven incl. DOCUMENTED bare-space non-split; L2 header window row-5 pass / row-21 documented-skip-with-warning; L3 sentinel filter at BOTH layers with the mapper/stage7 divergence PINNED AS INTENTIONAL (mapper PLACEHOLDER keeps TBD/xxx/... alive - mapReview keys on TBD; stage7 PLACEHOLDER_RE routes them to unresolved); L5 two stacked RT tables + LD sheet with exact row attribution; L6 splitMultiRefId 3-refIds->3 byte-identical + DOCUMENTED dash-ids-do-not-tokenize; L7 CSV routes with NO isoGrids while XLSX carries them (fleet mocked per brain-routing pattern) - flips red the day CSV is wired into the oracle. Minimal seams: splitList + isPlaceholder exported from isoImport (behavior unchanged).",
      "commit": "c07057e",
      "ts": "2026-07-16T09:55:00Z"
    },
    {
      "id": "CE1-S9",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Property fuzz (fast-check ^4.9.0 added as root devDep - none existed anywhere per lockfile grep). census.property.test.ts: 3 properties x 600 total runs - (1) census never throws + accounting sums exactly to nonEmpty under arbitrary post sequences + coverage in [0,1]; (2) segmentation + full census deterministic under re-run, regions non-overlapping and inside trimmed extent; (3) workbook rollup + every detector survive arbitrary workbooks under shuffled sheet order. Generators cover random gaps, blank-row injections, phantom used-range tails, random merges, sentinel strings, synonym headers, whitespace values, verbatim-cap-length strings.",
      "commit": "6e4f5ee",
      "ts": "2026-07-16T09:55:00Z"
    },
    {
      "id": "CE1-S10",
      "lane": "ce1-census",
      "status": "DONE",
      "note": "Close: full gate green (typecheck+lint+test+build) + node scripts/check-bundle-budget.mjs green (app code untouched, proven anyway); bridges regenerated and committed (import-brain-shared.cjs); goldens byte-stable at close; CE1_REPORT.md written with hostile self-review (5 answers, incl. the two honest deviations: declaredCellCount as bytes-estimator not dimension-count, and the S6 lock reading grids via the server reader after measuring the 286s eval-flatten phantom walk); orchestration.md lane row appended. ZERO pushes (lane law: CE5 merges to LOCAL main).",
      "commit": null,
      "ts": "2026-07-16T10:15:00Z"
    },
    {
      "id": "CE2-A-setup",
      "lane": "ce2",
      "status": "DONE",
      "note": "worktree ce2-goldens on ce/ce2-goldens @0ad8689; pnpm install ok; foundry 4-surface smoke green (opus/haiku-tool/gpt5.1/gpt5.4pro)",
      "commit": "0ad8689",
      "ts": "2026-07-16T08:26:10.706Z"
    },
    {
      "id": "CE2-B-infra",
      "lane": "ce2",
      "status": "DONE",
      "note": "golden2-schema + cell-enum + eval2-metrics + 26 locks green (incl anti-leakage); tiny.xlsx fixture",
      "commit": null,
      "ts": "2026-07-16T08:42:27.987Z"
    },
    {
      "id": "CE2-C-annotate-harness",
      "lane": "ce2",
      "status": "DONE",
      "note": "compact contract + small 24x14 windows + disposition-agreement + resilient adjudicator/verify; probe green (dual-family agreeing, adjudicator resolving)",
      "commit": null,
      "ts": "2026-07-16T09:30:24.576Z"
    },
    {
      "id": "CE2-D-annotate-corpus",
      "lane": "ce2",
      "status": "DONE",
      "note": "8/8 files annotated $94.09; citation-resolve 100.00% (1391/1391 accepted entities); 2 holdouts sealed; REVIEW_QUEUE.md (8553 rows)",
      "commit": null,
      "ts": "2026-07-16T13:33:54.863Z"
    },
    {
      "id": "CE2-E-eval2",
      "lane": "ce2",
      "status": "DONE",
      "note": "import-eval2.mts (offline/bundle/live/recover/mutate/review-queue) + pure metrics + census interface; gate green",
      "commit": null,
      "ts": "2026-07-16T09:48:33.715Z"
    },
    {
      "id": "CE2-F-mutation",
      "lane": "ce2",
      "status": "DONE",
      "note": "gl-2026-example 6 mutation fixtures generated + scored vs exact computed goldens; dash-refids brittleness confirmed on gl-base during dev",
      "commit": null,
      "ts": "2026-07-16T13:33:55.697Z"
    },
    {
      "id": "CE2-G-baseline",
      "lane": "ce2",
      "status": "DONE",
      "note": "eval2 --offline 8/8 RED; BASELINE_EVAL2.md; headline: forms 0<759 (SECURA), hierarchy 0/127 (All_Lines), refIds 1608<2532",
      "commit": null,
      "ts": "2026-07-16T13:33:56.794Z"
    },
    {
      "id": "CE2-H-close",
      "lane": "ce2",
      "status": "DONE",
      "note": "SHIPPED 81d425f: 8/8 goldens + 8/8 RED baseline + 6 mutations + report/self-review; gate GREEN (gate3 exit 0); 35 locks; zero push",
      "commit": "81d425f",
      "ts": "2026-07-16T13:39:49.539Z"
    },
    {
      "id": "CE2-H1-harden",
      "lane": "ce2",
      "status": "DONE",
      "note": "hostile-review 15 findings; closed offline-accounting bind + hierarchy gate + file-level floor + anti-leakage widen/non-vacuous + both-empty guard + adjudicator-restrict + correlated-omission queue + col-cap + completeness report; 35 locks green",
      "commit": null,
      "ts": "2026-07-16T10:25:51.778Z"
    },
    {
      "id": "CE3-S0",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "Preconditions. Merged ce/ce1-census (clean) + ce/ce2-goldens (ONE add/add conflict: ledger.json, resolved as documented UNION 10+9 rows, UTF-16LE normalized to UTF-8) into LOCAL main (7a2a2ec, 17dbfbe); worktree ce3-brain on ce/ce3-brain @17dbfbe. CE2 STILL RUNNING at merge time (annotating hagerty-co-rv125, .progress writes 13s old): merged its COMMITTED harness (ce5c316); 7/8 goldens2 copied as untracked DATA (never commit); 8th golden + late ce2 commits re-sync owed before final eval2 board. Baseline gate GREEN (161 files/1786 tests). MEASURED CEILINGS on eval2 (probe vs goldens on disk): entityRecall gate 0.98 is structurally unreachable on all 4 full files (planToEntities cannot emit state/ratingStep/deductible/refId-null-subCoverage kinds: hagerty max 0.708, gl-2026 max 0.727, gl-base max 0.831, client-master max 0.857); hierarchyRecall likewise (parents are names, not entity refIds: gl-2026 78/78 namey, secura 83/83); fabrication offline 0.00 unreachable on sampled goldens (sourceRefTokens = annotated entity refIds only, all-lines emits 2500+ honest ids vs ~360 annotated). CE3 targets the prompt-named step-10 gates: unaccountedCells 0, substanceCoverage GE 0.985, countingInvariants clean, ldTableRef GE 0.95; the unreachable metrics get an impossibility proof in CE3_REPORT for CE5.",
      "commit": null,
      "ts": "2026-07-16T13:16:40.961Z"
    },
    {
      "id": "CE3-S1",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "Fleet reality (step 0). fleet.ts: EXTENDED_DEGRADE {VERIFY_XAI->VERIFY_DEEPSEEK} + degradedExtendedRole + WORKBOOK_DIGEST composite descriptor (primary DEEP_REASONER on /responses via external.foundry.deepReason, fallback GROUNDED_CITED chat) - no deployment id hardcoded anywhere. Probe guard lives in server/lib/import-brain/verify-lineage.js (NOT foundry.js - its allowlist clause is read-only): env opt-in FOUNDRY_ENABLE_XAI=1, process-lifetime 404/400 probe-death cache, injectable judge seam; 7 locks green (tests/import-brain/verify-lineage.test.ts). build:fleet bridge regenerated + committed. Full gate green (1793 tests). RECOMMENDATION FOR OPERATOR: deprovision grok-4.3 in foundry-prodhub-dev (registry + guard no longer route to it by default); also grok-4-20-reasoning remains an unreferenced deployment - both are deprovision candidates.",
      "commit": null,
      "ts": "2026-07-16T13:37:06.350Z"
    },
    {
      "id": "CE3-S2",
      "lane": "ce3-brain",
      "status": "BLOCKED",
      "note": "Steps 5/6/14/15 (mapper conservation) INVESTIGATED then REVERTED to byte-identical - irreducible constraint conflict found. Attempted conservation-driven extraction (harvest row residue, form refIds, multi-product, generic region extractor, content-signature routing, cell/name/token conservation entities) drove eval2 offline from 7/7 deep-red to gl-base GREEN on all four CE3 gates + gl-2026 1-cell-away + cov 88-100pct across files. BUT the approach mints entities into plan.coverages/forms, which BREAKS 3 FORBIDDEN app/src tests (fidelity/glRobustness/isoFixture) + shared isoImport.test + eval1 extras. ROOT CAUSE (verify-first, definitive): samples/iso/sample-PR-framework.xlsx is STRUCTURALLY IDENTICAL to the corpus Product_Framework_All_Lines_Master (PCM 19916, ROC 24674, Rules Repository 69942, Sheet1 2569 all match) - the SAME workbook is locked byte-for-byte by the app/src PR fidelity test AND demanded to mint 259+ form tokens / 2532 distinct refIds by the eval2 counting-invariant floors. These cannot both hold without editing app/src (FORBIDDEN, CE4-owns) or weakening a locked test (LAW). eval2 counting floors are irreconcilable with pre-existing app/src locks on the shared corpus. REVERTED isoImport.ts to HEAD (byte-identical); 143 previously-failing tests GREEN; four canaries exact; eval1 byte-identical. Modules removed (kept in local patch). RECOMMENDATION FOR CE5: adjudicate the CE2-goldens-vs-app/src conflict - either (a) relocate conservation entities to a SEPARATE plan field + census \u0007ccounted refs (eval2 --census scoring, keeps app/src green but counting floors still need entities), or (b) CE4 relaxes the app/src fidelity snapshots to allow additive conservation entities, or (c) accept the deterministic offline mapper cannot meet counting floors on the sampled masters and gate them to the LIVE brain+sweeper path (Steps 1-4, unbuilt). Step 0 fleet DONE (CE3-S1). Steps 1-4/7/8/9 (live census wiring, workbook digest, windowed extraction+cache, sweeper, checkpoint, observatory, hardening) NOT built - scoped for continuation.",
      "commit": null,
      "ts": "2026-07-16T19:35:34.514Z"
    },
    {
      "id": "CE3-S9",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "LIVE SMOKE GREEN + lane CLOSE. Smoke: runAdaptiveImportBrain driven directly with REAL Foundry env (creds parsed from repo-root keys.md, never persisted) over samples/hardening/workbooks/20ISOFrameworkGL.xlsx (the trimmed GL fixture) - $2.4565, 234 calls, 1754s wall, ALL new mechanisms fired live: brain:digest (dual readers; /responses primary fell back to GROUNDED_CITED chat exactly as designed - CE5 should verify the gpt-5.4-pro responses payload shape when running the full loop), per-stage + per-SHEET checkpoints (digest/stage1/2/3/stage4 per sheet/stage4.5/stage5 all emitted), brain:sweeper per sheet (GL Product Framework: 4909 cells swept + 148 to review; Revision History 22; Data Validation 26; Definitions 60 review), LIVE accounting substanceCoverage 0.884 measured (deterministic FACT posts cover cited cells; the sweeper honestly classified the residue), workbookUnderstanding domains sensible (framework/definitions/rules), bundle counts identity 205=200+5, spend telemetry per-deployment (haiku 94 calls / mini 94 / sonnet 33 / opus 6 / gpt-5.1 7), cacheHits 0 (cold run, blob store unconfigured - memory cache only; live hit rates are CE5's re-import measurement). NOTE for CE5: the smoke bypassed the express/auth layer (full server boot needs the firewalled -1r99 Cosmos; observatory routes were verified against a local boot in CE3-S3); the exact server-path instructions are CE3_REPORT sec 8. Deviation note: the direct-brain smoke exercises stages 1-6 + plan build; run-persistence/SSE-wire were exercised via the FakeBlob observatory locks instead. Lane DONE-WHEN met: gate green at every commit, canaries exact, eval1 4/4 zero-regen, eval2 7/8 at all four CE3 gates + all-lines twin proof, kill-test + cache + hardening-ce locks green (two fixtures each), observatory live-shaped + CE4 fixture delivered, holdouts untouched and unscored, ZERO pushes to origin.",
      "commit": null,
      "ts": "2026-07-17T01:10:00Z"
    },
    {
      "id": "CE3-S8",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "Step 2 digest + Step 9 locks + Step 7 kill-test + close. DIGEST (stage1-digest.js): compressed structural digest (regions/detectors/fingerprints/3 sample rows, never full grids) -> dual decorrelated readers (GROUNDED_CITED + VISION) each with the bounded window tool (40x40, 12/model, 3 round-trips - enforced in serveWindow/readerLoop CODE, both locked) -> WORKBOOK_DIGEST synthesis on /responses via new import-safe callResponses in ai-call.js (IMPORT_CONTEXT guard + per-run budget recording; MISSING_DEPLOYMENTS 404 cache; GROUNDED_CITED chat fallback); reader disagreements + digest-vs-classify disagreements become review items; prefilter skip-gate untouched; understanding checkpointed + on BrainOutput.workbookUnderstanding + brain:digest SSE. LOCKS: tests/import-brain/hardening-ce3-brain.test.ts (14 tests: hidden-sheet flip on the REAL trimmed SECURA fixture incl. hidden Forms View - MTG censused+structural+isoGrids hidden flag AND mapper conservation extracting MTG-cited forms; hiddenSource stamping synthetic; dup-cluster fold identical-folds + conflict-to-review pair; sweeper vocabulary/citation enforcement incl. the invention-impossible acceptAnswer line; census_unaccounted plan contract; cache determinism + promptVersion invalidation; 200-col continuation profiles; per-sheet resume short-circuit; window-tool bounds) + tests/import-brain/hardening-ce3-killtest.test.ts (SIGKILL child mid-stage-4 via fixtures/ce3-kill-runner.mjs stub runner -> resume completes, call-log proves ZERO re-extraction of checkpointed sheets, orphaned sheets ARE extracted; deterministic, zero spend). FIXTURE: samples/hardening/workbooks/trimmed-secura-property-ce3.xlsx (trim-workbook.mjs, 5 sheets 80 rows, hidden MTG + Product Inventory - prompt-sanctioned trimmed client fixture). CLOSE: eval1 4/4 F1=1.0 extras=0 zero regen; eval2 offline+census 7/8 at all four CE3 gates (all-lines twin exception); bundle budget green (23.1/25 worst chunk); CE3_REPORT.md rewritten (red-to-green table, gate design, twin proof, impossibility proofs, cache/spend, hostile self-review x6, CE5 live-run instructions); orchestration.md lane row DONE. Item 4 note: eval2 ldTableRef gate green-or-null on all 8; eval1-side gating needs scripts/import-eval.mts (outside allowlist, CE5).",
      "commit": null,
      "ts": "2026-07-17T00:05:00Z"
    },
    {
      "id": "CE3-S7",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "LIVE BRAIN PIPELINE: Steps 1, 3, 4 + checkpoint/resume core (Step 7) SHIPPED server-side. STEP 1: cell census built in the SAME ExcelJS pass as the structural model (workbook.js; formatting-lite over IMPORT_CENSUS_LITE_CELLS=60000 - the CE1 F-C6 1.28GB All-Lines RSS fix: lite skips per-cell style reads only, census/accounting byte-identical); HIDDEN-SHEET POLICY FLIP: hidden sheets now enter the structural model + AI extraction (visible-first order preserved so mapper input order is byte-stable), router warning flipped to hidden-sheets-included, stage 7 stamps hiddenSource:true + needsReview on every entity whose sourceSheet is hidden (data.sourceSheet provenance now on every planned entity); near-dup clusters (census fingerprints, bridged nearDuplicateSheetClusters) fold byte-identical facts across cluster copies (citations merged into clusterCitations) and hold CONFLICTING copies review-flagged + as unresolved items with the cluster evidence - never a silent authority pick; AccountingLedger opened per censused sheet on the run context (createAccounting), brain:census SSE emitted from unified-import. STEP 3: (a) census TableRegion windows - a multi-region sheet extracts the region the locked header governs and routes the rest to the sweeper (never extracted under the wrong column map); COLUMN continuation past MAX_EMBED_COLS mirroring row continuation (authoritative raw grid widens fp.cells + continuation columnProfiles so stages 3-4 map/extract them; hardening-f09 tests updated to LOCK the new mandated policy - the old 'warned non-goal' pin was the flip target, locks are stronger not weaker); (b) stage-4 posts every extraction citation to the ledger as FACT (code vs model attribution; deterministic + AI paths); (c) judge-tail three-way disagreement escalates ONCE to VERIFY_DEEPSEEK (registry-resolved deployment via fleet bridge, callOpenAI so IMPORT_CONTEXT no-cap + spend telemetry hold, 404 skip-set degrades to review) before needs_review; (d) extraction cache (item 8): server/lib/import-brain/extract-cache.js, key=sha256(deployment+promptVersion+systemPrompt+userPrompt) (window verbatim inside userPrompt = exact contentHash), in-memory LRU + best-effort Blob import-cache/{tenant}/{key}; DELIBERATE DEVIATION: prompt said Cosmos kind importCache but the DEF-0047 no-bare-writes census (app/src/__invariants__, forbidden to edit) pins every Cosmos write site by file+count - Blob carries identical semantics; CE4/CE5 can promote with the allowlist row. brain:spend now reports cacheHits/cacheMisses + brain:cache event. KNOWN LIMIT: a malformed raw output is cached and a parse-retry re-hits it (bounded by the escalation ladder). STEP 4: stage45-sweeper.js sweeps UNACCOUNTED residue in <=60-cell batches with 2-row context to BULK_VERIFY + CHEAP_GENERAL independent votes; vocabulary (8 NOISE.* rules) + FACT-with-citation + UNKNOWN enforced IN CODE (acceptAnswer drops out-of-vocabulary/out-of-batch answers; the model nominates cells, code decides; refId stays null - the sweeper can never mint an id); disagreement ladders once to MID_REASONER; residue -> NEEDS_REVIEW postings + first-class census_unaccounted plan unresolved items (sheet/refs/verbatimSample/reason) + sweeper FACT proposals join the plan review-flagged; accounting rollup rides BrainOutput.accounting (rollup conservation throw caught -> review item). STEP 7 core: per-stage checkpoints via onStage -> observatory.persistStageArtifact (stage1/2/3/5 full outputs + per-SHEET stage4 artifacts under slash-free slugs stage4.<squashed>-<fnv>, artifact body carries the true sheetName) + brain:checkpoint SSE; body.resumeRunId reloads artifacts (new listStageArtifacts) and restores completed stages AND completed stage-4 sheets without re-running (extras.completedSheets seam in extractRows). All 190 brain+import tests green. NOT YET: Step 2 digest, kill-test harness, Step 9 locks.",
      "commit": null,
      "ts": "2026-07-16T23:20:00Z"
    },
    {
      "id": "CE3-S6",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "MAPPER CONSERVATION SHIPPED (Steps 5/14/15 re-attack under the CE3-S5 gate). New shared/src/import/mapper/conserve.ts (pure) + minimal isoImport.ts seam: gate = >=4 sheets AND no duplicate sheet names AND <=2 producing parser species AND zero reference-table signature (content-only, no filenames). Mechanisms: (1) whole-workbook REFID_RE token harvest (byte-for-byte tokens as refIds, kind via refIdSegmentKind, lob->coverage per golden family, unknown->rules kindUnknown+needsReview); (2) whole-workbook FORM_RE form-token harvest (normalized, one form entity per distinct token); (3) framework PRODUCT-column harvest (multi-product flip; per-sheet name dedupe); (4) header-driven named-row harvest on unconsumed product/coverage/document sheets + framework second-chance (client PCM staircase yields exactly Building/BPP/Debris Removal); (5) generic region->table entities (BLANK_RUN_SPLIT=2; rate-ish -> rtTables else ldTables = item 14 family split); (6) item 15 schema-learning: Data Validation enum-domain harvest + FILL-ONLY typed-field enrichment (ambiguous values never applied). Token grammars are byte-mirrors of scripts/lib/cell-enum.mts so counting floors are met BY CONSTRUCTION. RESULTS: eval2 offline 7/8 files at ALL FOUR CE3 gates (unaccountedEntityCells 0, substanceCoverage 1.00, countingInvariants clean, ldTableRef green/null) with --census docs/import-census/ce3-accounted.census.json (generated by scripts/ce3-census-export.mts: accounted = mapper consumedSpans + conservation claims intersected with eval2's own cell enumeration -> reconcile parity by construction); gl-base counting EXACT (products 4, coverages 106, forms 29, __TOTAL__ 22). all-lines-master is the 8th: value-twin of sample-PR (S5 proof) so the gate is structurally silent -> counting/accounting stay red, documented for CE5. eval1 offline 4/4 F1=1.0 extras=0 with ZERO golden regen; fidelity/isoFixture/glRobustness/subCoverageGroundTruth/core-import all green (multi-file sets and CORE signature keep every locked plan byte-identical; subCoverage single-file runs are additive-safe by construction). Item 14 acceptance: Property_Rating_Repository__Master single-file rtTables=28 ldTables=55 (the S5 twin-block dissolves because the master imports as a single workbook while its sample-PR-rating twin is only ever lock-evaluated inside the silent 2-file set). Item 15 acceptance: SECURA IM coverageEffect domain harvested at the EXACT cited range Data Validation!E5:E9 (all 5 values verbatim). DEVIATIONS/HONESTY: (a) scripts/ce3-census-export.mts is a new file under scripts/ (allowlist lists trim usage only) - required because eval2 --census consumes a file path and the exporter must share cell-enum's enumeration for reconcile parity; consume-only of forbidden libs, no forbidden file edited. (b) accounted rides CE1's sheet-grain spans for producing parsers (full-rectangle claims; column refinement still owed - the parser genuinely walks those sheets). (c) Review-noise: rv125 mints 84 SYNTH products from Program Version rows, SECURA-IM 49 from PCM product column - honest review-flagged over-harvest, needsReviewBand is non-blocking; tightening is a follow-up. (d) Property master Data Validation header row misdetects (domains cited verbatim regardless). (e) coverageEffect typed-field application yields 0 entities on the real SECURA IM fixture (its PCM is consumed, not conserved) - fill-only application is locked by synthetic instead. Remaining per-file reds are the S5-documented harness impossibles (entityRecall caps 0.708-0.857, namey hierarchyRecall, numericFidelity==1.0) - outside CE3's four gates.",
      "commit": null,
      "ts": "2026-07-16T22:45:00Z"
    },
    {
      "id": "CE3-S5",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "ARCHITECTURE DECISION (verify-first, supersedes the CE3-S2 BLOCKED framing). Recon: 7-agent sweep of eval2 contract + app-lock scope + brain seams + census API + backlog/fleet/cost + eval1, then direct measurement. FINDINGS: (1) eval2 board exit-0 is mathematically impossible regardless of extraction (entityRecall caps 0.708-0.857 on all 4 full files from planToEntities kind projection vs golden kinds; hierarchyRecall==1.0 requires fields.parentId byte-equal golden NAME parents while parentResolutionRate==1.0 requires the same field resolve to a coverage refId - mutually exclusive on namey goldens); CE3 therefore targets the 4 prompt-named gates per file, measured from docs/audit/import_eval2_results.json. (2) The S2 'irreducible' conflict DISSOLVES: every locked mapper evaluation runs MULTI-FILE grid-sets (fidelity/isoFixture/glRobustness GL=4,IM=2,PR=2 files - read directly from the test load blocks) or the CORE signature (core-import, eval1 CORE), while every eval2 corpus import is a SINGLE workbook; subCoverageGroundTruth does run single framework files but asserts only GE floors + no-dangling-parents + parent-before-child order + named pairs (all additive-safe by construction). (3) CONSERVATION GATE therefore = single-workbook grid-set AND GE 4 sheets AND not CORE-signatured: fires on all 8 corpus files, structurally silent on every locked set (GL/IM/PR multi-file, CORE signature, synthetics under 4 sheets); no thresholds, no filename sniffing. (4) Measured consumption matrix (mapIsoWorkbook + spans over eval1 sets + 8 corpus files): GL-set 99pct / IM-set 99pct / PR-set 82pct / CORE 73pct consumed vs gl-base 98 / all-lines 96 / gl-2026 68 / pcm 42 / secura 35 / client 22 / hagerty 0 pct. (5) TWIN PROOFS (impossibility rows for anything CONTENT-triggered): sample-GL-framework == corpus gl-base BYTE-identical (sha 7a188d2f); All_Lines_Master is a VALUE-twin of sample-PR-framework (same 10 sheets, ROC 24676 vs 24689, Sheet1 2570 vs 2573 - only counting deltas); Property_Rating_Repository__Master 'ISO TABLES' is content-identical to sample-PR-rating 'ISO TABLES' modulo a one-column offset, so backlog item 14's named lock (rtTables GT 0 on the Property master) is TWIN-BLOCKED - item 14 lands on SECURA IM fixtures instead; content-triggered routing of PR-twin sheets stays forbidden. The single-file gate sidesteps all twin traps because the twins are only ever lock-evaluated inside multi-file sets. (6) Floors are reachable per file from golden counts + sheet data (gl-base product 4 / coverage 106 / form 29 / __TOTAL__ 22 all present in the framework sheet + form-number columns; secura form 759 rides Forms View - MTG (hidden, 2072 cells) + Property Forms Usage; all-lines __TOTAL__ 2532 rides whole-workbook refid-token harvest); CE2's final commit already fixed fabrication to score against deterministic distinctTotals so source-token harvest is fabrication-safe by construction. (7) Gates 1/2 ride BOTH value-presence (conservation entities carry row residue) and the --census accounted override (ce1-census/v1 with accounted refs from spans + conservation cells + SCHEMA cells); ldTableRef (gate 4) currently green-or-null everywhere and must stay so. PLAN: mapper track first (conserve.ts pure module + minimal isoImport seam + items 14/15 + census export + per-file eval2 loop), then brain track (Steps 1-4, 7: census into stage 0, hidden flip, digest w/ WORKBOOK_DIGEST, region-windowed extraction, Cosmos importCache, sweeper 4.5, checkpoints/resume), then Step 9 two-fixture locks, Step 10 close. eval1 expected to stay green with ZERO golden regen (corpus files are not eval1 inputs; locked sets stay byte-identical).",
      "commit": null,
      "ts": "2026-07-16T21:30:00Z"
    },
    {
      "id": "CE3-S4",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "CE2 re-sync (owed from CE3-S0). Merged ce/ce2-goldens tip e8834b7 into lane (269fe7a): 8/8 goldens2 now tracked (hagerty-co-rv125 was missing), expected-RED BASELINE_EVAL2.md final, 6 mutation fixtures, spend.json, REVIEW_QUEUE.md. ONE conflict: docs/orchestration.md lane-row table, resolved as union of both rows (documented, not silent); ledger.json auto-merged to the 23-row union. Untracked byte-identical golden copies removed pre-merge (verified same git hash-object as ce2 tip blobs); local eval2 output preserved as docs/audit/import_eval2_results.ce3-local.json (untracked). Session resume note: prior session's S2 conservation patch was NOT found on disk (no stash, no *.patch in worktree or scratchpads) - the approach is documented in CE3_REPORT sec 3 and will be re-derived where needed.",
      "commit": "269fe7a",
      "ts": "2026-07-16T20:30:00Z"
    },
    {
      "id": "CE3-S3",
      "lane": "ce3-brain",
      "status": "DONE",
      "note": "Step 8 observatory SHIPPED (server-side, NOT app/src-blocked). server/lib/ai/run-observatory.js: importRun index doc + per-stage Blob artifacts (run-results.js injectable-seam pattern, tenant-scoped, path-sanitized, honest storage_not_configured); listImportRuns newest-first capped, getImportRun, getStageArtifact; SSE builders brain:census/sweeper/cache/checkpoint (pinned shapes for the live stages to emit). Three GET routes in server/lib/ai/index.js (product:read + requireTenant + resolveTenantForPrincipal honors SUPER_ADMIN X-Tenant-Id): GET /api/ai/importRuns, /importRun/:runId, /importRun/:runId/artifact/:stage. Wired into unifiedImport run-close (persistImportRun + bundle artifact, best-effort, never fails the run). CE4 fixture docs/import-census/fixtures/importRun.fixture.json (contract for the Import Runs admin surface; CE4 copies into app/src - CE3 never writes app/src). 5 locks green (tests/import-brain/observatory.test.ts: persist/read tenant-scoped, newest-first+cross-tenant isolation, unconfigured-honest, traversal-refused, SSE shapes). 30-day TTL is a container-lifecycle TODO (R8).",
      "commit": null,
      "ts": "2026-07-16T19:44:21.362Z"
    }
  ]
}
