{
  "name": "prodhub-azure-host",
  "version": "2.0.0",
  "private": true,
  "description": "Azure App Service host for Product Hub — serves the Vite SPA and the full /api backend (auth, Cosmos data, AI). Replaces Firebase/GCloud.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/storage-blob": "^12.26.0",
    "compression": "^1.7.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "nodemailer": "^6.9.16"
  }
}
