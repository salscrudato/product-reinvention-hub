# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
# tsup output; not needed in repo
/functions/lib/
.svelte-kit/

# secrets — NEVER commit
# .env.local and .env.*.local are overrides (secret); .env.development etc. are shared (committed)
.env.local
.env.*.local
*.local
keys.md
# Any scratch/secret markdown dropped at the repo root (never commit these)
tmp*.md

# Playwright e2e artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# IDE
.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
Thumbs.db

# Codebase review pack — keep ONLY the markdown deliverables; ignore heavy/generated artifacts
docs/review/*
!docs/review/FUNCTIONAL_CODEBASE.md
!docs/review/OBSERVATIONS.md
!docs/review/ENHANCEMENT_BACKLOG.md
!docs/review/COST_BASELINE.md
!docs/review/COST_PROGRESS.md
!docs/review/COST_REPORT.md
docs/audit/import_live_results.json
docs/audit/import_ledger.json

# Client-proprietary sample workbooks used for local import eval only (never commit)
additional_samples/

# Headless orchestration session logs (local only)
tools/orchestrate/run/
