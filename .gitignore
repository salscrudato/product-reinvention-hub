# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
# tsup output; not needed in repo
/functions/lib/
.svelte-kit/

# secrets — NEVER commit
# .env.local and .env.*.local are overrides (secret); .env.development etc. are shared (committed)
.env.local
.env.*.local
*.local
keys.md
# Any scratch/secret markdown dropped at the repo root (never commit these)
tmp*.md

# Playwright e2e artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# IDE
.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
Thumbs.db

# Firebase local cache + scratch status file (never commit)
.firebase/
status.txt

